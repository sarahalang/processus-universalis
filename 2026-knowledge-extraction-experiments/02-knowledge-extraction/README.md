# 02. Knowledge Extraction Phase

Once the text is segmented into logical process steps, this phase handles the extraction of structured data using Large Language Models (LLMs).

---

### Project Structure

```text
02-knowledge-extraction/
├── extract_opac_knowledge.py      # LLM engine to extract SVEK patterns
└── ie_reconstruction.py           # Rebuilds procedural sequences from entities
```
